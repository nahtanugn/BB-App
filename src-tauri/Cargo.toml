[package]
name = "bb-company-app"
version = "1.1.0"
description = "Desktop edition of the BB Company App"
authors = ["BB Company App contributors"]
license = "MIT"
repository = "https://github.com/your-org/bb-company-app"
edition = "2021"

[lib]
name = "bb_company_app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
